export default function NotesPage() {
  return (
    <div>
      <h1 className="text-xl font-semibold">Notes</h1>
      <p className="text-muted-foreground mt-1 text-sm">
        Markdownメモ（期限なしToDo）一覧（横断検索）
      </p>
    </div>
  );
}
